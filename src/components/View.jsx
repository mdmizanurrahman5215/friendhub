import { Car } from "lucide-react";
import React, { Suspense } from "react";
import Cards from "./Cards";

const View = () => {
  return (
    <div>
      <Cards />
    </div>
  );
};

export default View;
